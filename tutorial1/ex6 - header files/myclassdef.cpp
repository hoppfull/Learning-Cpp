#include <iostream>

#include "myheader.h" //All source files connected to this header files need this #include

/* Here we define functions declared in header files. Not sure if
I can define more than one functions. We'll see. */

MyClass::MyClass() // :: = "binary scope resolution operator"
{
    //Define constructor
}
